@page "/demos/streaming/server"
@rendermode InteractiveServer
@implements IDisposable

@inject IStreamingService StreamingService

<PageTitle>Streaming — Server</PageTitle>

<div class="demo-header">
    <span class="badge bg-primary">🖥 Interactive Server</span>
    <span class="badge bg-secondary ms-2">IStreamingService → StreamingService (direct, no HTTP)</span>
</div>

<h2 class="mt-3">Live Item Streaming</h2>
<p>
    Items are yielded one by one from <code>StreamingService</code> via server DI — SignalR pushes
    each <code>StateHasChanged()</code> to the browser as it arrives.
</p>

<div class="row g-2 align-items-center mb-3">
    <div class="col-auto">
        <label class="col-form-label">Items to stream</label>
    </div>
    <div class="col-auto">
        <input type="number" class="form-control" style="width:90px"
               @bind="_count" min="1" max="100" disabled="@_running" />
    </div>
    <div class="col-auto">
        <button class="btn btn-primary" @onclick="StartAsync" disabled="@_running">▶ Start</button>
    </div>
    <div class="col-auto">
        <button class="btn btn-danger" @onclick="Cancel" disabled="@(!_running)">⏹ Cancel</button>
    </div>
    <div class="col-auto">
        <button class="btn btn-outline-secondary" @onclick="Reset" disabled="@_running">↺ Reset</button>
    </div>
</div>

@if (_items.Count > 0 || _running)
{
    <div class="mb-2 d-flex gap-2 align-items-center">
        <span class="badge bg-info text-dark">@_items.Count / @_count received</span>
        @if (_running)
        {
            <span class="badge bg-warning text-dark">⏳ Streaming…</span>
        }
        else if (_cancelled)
        {
            <span class="badge bg-danger">⏹ Cancelled after @_items.Count items</span>
        }
        else
        {
            <span class="badge bg-success">✓ Done</span>
        }
    </div>

    <table class="table table-sm table-striped" style="max-width:520px">
        <thead>
            <tr>
                <th style="width:50px">#</th>
                <th>Message</th>
                <th>Arrived at</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in _items)
            {
                <tr>
                    <td>@item.Index</td>
                    <td>@item.Message</td>
                    <td>@item.Timestamp.ToString("HH:mm:ss.fff")</td>
                </tr>
            }
        </tbody>
    </table>
}

@code {
    private readonly List<StreamItemDto> _items = [];
    private CancellationTokenSource? _cts;
    private bool _running;
    private bool _cancelled;
    private int _count = 20;

    private async Task StartAsync()
    {
        _items.Clear();
        _cancelled = false;
        _running = true;
        _cts = new CancellationTokenSource();

        try
        {
            await foreach (var item in StreamingService.StreamItemsAsync(_count, _cts.Token))
            {
                _items.Add(item);
                StateHasChanged();
            }
        }
        catch (OperationCanceledException)
        {
            _cancelled = true;
        }
        finally
        {
            _running = false;
            StateHasChanged();
        }
    }

    private void Cancel() => _cts?.Cancel();

    private void Reset()
    {
        _items.Clear();
        _cancelled = false;
    }

    public void Dispose()
    {
        _cts?.Cancel();
        _cts?.Dispose();
    }
}
